# App Data

This directory is used by the local app at runtime.

Generated JSON files are intentionally ignored in the public repository:

- `app-data.json`
- `model_photo_overrides.json`

Use private/local data generation during development, or provide a sanitized sample dataset for a public demo.
